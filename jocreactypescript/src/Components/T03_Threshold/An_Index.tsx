type TS_Threshold={
// Properties of Each Column
    Key:number,         // DEFAULT: Math.random()
                        // Unique Key
    PositionY:number,
    IsDefault:boolean,
    Gray:string
}

export default TS_Threshold